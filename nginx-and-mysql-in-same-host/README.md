# NGINX with PHP 7.4 and MySQL

Before all, clone this repository and navigate to the directory where it was cloned

Running

```bash
docker compose up -d
```

Stopping and destroying

```bash
docker compose down -t 0 --rmi all
```
